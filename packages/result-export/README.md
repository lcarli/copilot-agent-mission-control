# Result export

Builds deterministic, versioned JSON exports from explicit event, unit,
mission, score, adjustment, and recognition projections. Output is allowlisted,
idempotent, checksum-addressed, and excludes credentials, prompts, source code,
and internal telemetry by construction.
