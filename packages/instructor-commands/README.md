# Instructor commands

Closed, versioned command handling for event closure, incident modifiers,
mission overrides, score corrections, and unit moderation. Accepted and
rejected commands are idempotently audited with optimistic concurrency.
