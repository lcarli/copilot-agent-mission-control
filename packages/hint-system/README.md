# Hint system

Delivers campaign-defined hints progressively for an active unit mission and
records immutable, idempotent usage. Delivery returns the localization key and
the versioned bonus adjustment; it never changes mission completion state.
